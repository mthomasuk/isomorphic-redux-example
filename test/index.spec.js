var routes = require('../routes');
var reducers = require('../reducers');

test('should pass', () => {
    // Dont delete, used to make sure coverage includes all needed files
    expect(true).toBe(true);
});
